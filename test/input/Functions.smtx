main module functions {

    @decl(global, int);

    (argument: int) {
        @decl(local, int);

        @printint(local);
        @printint(global);
        @printint(argument);

        {
            @printstring("nested 1\n");
            @printint(local);
            @printint(global);
            @printint(argument);
        };

    };

}